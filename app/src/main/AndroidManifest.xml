<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qtasnim.qhopes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.RegisterDokterActivity"></activity>
        <activity android:name=".navigation.fragments.MenuSpesialisActivity" />
        <activity android:name=".activities.MenuMingguiniActivity" />
        <activity
            android:name=".activities.SplashscreenActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LandingActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.MenuHariIniActivity" />
        <activity android:name=".activities.MenuBeritaActivity" />
        <activity android:name=".activities.MenuKontakActivity" />
        <activity android:name=".activities.NavPendaftaranFormActivity" />
        <activity android:name=".activities.MenuInfoActivity" />
    </application>

</manifest>